services:
  vllm-server:
    image: nvcr.io/nvidia/vllm:26.01-py3
    container_name: vllm
    restart: "no"

    expose:
      - "8000"

    shm_size: "16g"

    stop_grace_period: 2m

    ulimits:
      memlock: -1
      stack: 67108864

    mem_limit: "80g"
    memswap_limit: "90g"

    gpus: all

    volumes:
      - ../../models/Ministral-3-14B-Instruct-2512:/models/Ministral-3-14B-Instruct-2512:ro

    env_file:
      - ../../.env

    # Source: https://huggingface.co/mistralai/Ministral-3B-Instruct-2501
    # --tokenizer_mode/--config_format/--load_format mistral: required to use
    #   Mistral's native weight format. Without these, vLLM falls back to the
    #   HuggingFace loader which misidentifies this model as a Pixtral vision
    #   model (PixtralForConditionalGeneration), causing startup to crash.
    # --enable-auto-tool-choice / --tool-call-parser mistral: required for
    #   function/tool calling support.
    # --max-model-len: caps total prompt+generation tokens per request.
    #   Set to 32768 as a reasonable upper bound. NOTE: the definitive fix
    #   for runaway generation is max_tokens in each client API call.
    #   Ministral-8B with xgrammar structured output never terminates
    #   naturally — it generates until this cap is hit. Client code must
    #   pass max_tokens (e.g. 512) on all structured-output calls.
    command: >
      vllm serve /models/Ministral-3-14B-Instruct-2512
      --host 0.0.0.0
      --port 8000
      --tokenizer_mode mistral
      --config_format mistral
      --load_format mistral
      --enable-auto-tool-choice
      --tool-call-parser mistral
      --gpu-memory-utilization 0.70
      --max-model-len 8192
      --max-num-batched-tokens 2048
      --max-num-seqs 32
      --swap-space 8

    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8000/health"]
      interval: 15s
      timeout: 10s
      start_period: 10m
      retries: 60

    networks:
      - internal

  caddy:
    image: caddy:latest
    container_name: caddy
    restart: unless-stopped
    depends_on:
      vllm-server:
        condition: service_healthy

    ports:
      - "80:80"
      - "443:443"

    mem_limit: "512m"
    memswap_limit: "512m"

    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

    networks:
      - internal

networks:
  internal:
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
