# Stop the LLM container when any GPU reaches this temperature (°C).
# Default: 80
THERMAL_THRESHOLD_C=80

# How often to poll GPU temperatures in seconds.
# Default: 5
THERMAL_POLL_SECONDS=5

# Name of the LLM container to stop on overtemp.
# Default: vllm
CONTAINER_NAME=vllm
